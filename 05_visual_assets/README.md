# Local Visual Assets

Place appropriately licensed source MP4 clips in:

- `cinematic/` — assets for `CIN` scenes
- `motion_graphics/` — assets for `MG` scenes

These media directories are intentionally excluded from the public repository. Keep only assets that you have permission to use locally, and maintain provenance records outside Git when required.
